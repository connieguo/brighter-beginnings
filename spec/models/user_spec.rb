require 'spec_helper'

describe User do
  pending "add some examples to (or delete) #{__FILE__}"
  
  describe "delete user" do
  
    it "returns false if the logged in account does not have valid permissions to delete target account" do
    end
  
    it "returns true if the logged in account successfully deletes target account" do
    end  
  
    it "deletes the appropriate accounts" do
    end
  end
  
end
